$schema: https://json-schema.org/draft/2020-12/schema
$id: PaymentFailed
description: Payment failed domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.payment.failed.v1
      data:
        $ref: ./payment-failed.data.yaml
    required:
      - type
      - data
