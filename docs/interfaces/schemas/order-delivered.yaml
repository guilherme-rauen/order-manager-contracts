$schema: https://json-schema.org/draft/2020-12/schema
$id: OrderDelivered
description: Order delivered domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.order.delivered.v1
      data:
        $ref: ./order-delivered.data.yaml
    required:
      - type
      - data
