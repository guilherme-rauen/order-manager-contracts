$schema: https://json-schema.org/draft/2020-12/schema
$id: PaymentRequested
description: Payment requested domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.payment.requested.v1
      data:
        $ref: ./payment-requested.data.yaml
    required:
      - type
      - data
