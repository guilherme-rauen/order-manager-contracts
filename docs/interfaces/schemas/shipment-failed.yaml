$schema: https://json-schema.org/draft/2020-12/schema
$id: ShipmentFailed
description: Shipment failed domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.shipment.failed.v1
      data:
        $ref: ./shipment-failed.data.yaml
    required:
      - type
      - data
