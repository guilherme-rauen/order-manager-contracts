$schema: https://json-schema.org/draft/2020-12/schema
$id: ShipmentDispatched
description: Shipment dispatched domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.shipment.dispatched.v1
      data:
        $ref: ./shipment-dispatched.data.yaml
    required:
      - type
      - data
