$schema: https://json-schema.org/draft/2020-12/schema
$id: SagaStepCompleted
description: Saga step completed domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.saga.step.completed.v1
      data:
        $ref: ./saga-step-completed.data.yaml
    required:
      - type
      - data
