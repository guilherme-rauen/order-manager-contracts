$schema: https://json-schema.org/draft/2020-12/schema
$id: ShipmentDelivered
description: Shipment delivered domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.shipment.delivered.v1
      data:
        $ref: ./shipment-delivered.data.yaml
    required:
      - type
      - data
