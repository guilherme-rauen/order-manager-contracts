$schema: https://json-schema.org/draft/2020-12/schema
$id: PaymentConfirmed
description: Payment confirmed domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.payment.confirmed.v1
      data:
        $ref: ./payment-confirmed.data.yaml
    required:
      - type
      - data
