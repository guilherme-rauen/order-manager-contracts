$schema: https://json-schema.org/draft/2020-12/schema
$id: SagaStarted
description: Saga started domain event (full message including CloudEvents envelope)
allOf:
  - $ref: ./envelope.yaml
  - type: object
    properties:
      type:
        const: com.ordermanager.saga.started.v1
      data:
        $ref: ./saga-started.data.yaml
    required:
      - type
      - data
